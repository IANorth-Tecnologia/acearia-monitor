export interface DashboardData {
    status: string;
    mensagem: string;
    perigo: boolean;
    panelas: number;
    garras: number;
    
}

export interface SafetyStats {
    panelas: number;
    garras: number;
}
